import { type CustomResourceNodeType } from './types'
import type { ResourceDefault } from '@/app/components/taskStream/types'

const customResourceDefaults: ResourceDefault<CustomResourceNodeType> = {
  defaultValue: {
  },
}

export default customResourceDefaults
